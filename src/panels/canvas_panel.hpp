#pragma once

namespace shadebug { class App; }

namespace shadebug::panels {

/// Renders the main page canvas with responsive fit-to-window layout.
void draw_canvas_panel(shadebug::App& app);

} // namespace shadebug::panels

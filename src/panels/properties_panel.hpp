#pragma once

namespace shadebug { class App; }

namespace shadebug::panels {

/// Renders the element / page properties panel.
void draw_properties_panel(shadebug::App& app);

} // namespace shadebug::panels

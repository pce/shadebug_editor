


> The trick is: push almost everything to the GPU, and keep the CPU side as a thin command buffer.


draft of a not visual-node based UI
\ <br> 
 Spatial UI, not scalar UI with a simple list based editor with motion/spatial ui for shader:

- parameters might be a good start

== Signed Distance Fields

Motion Input for Parameters to understand how to draw things on the GPU:

- u_width, u_height
- u_corner_radius
- u_border_width
- u_fill_color (r,g,b)
- u_border_color (r,g,b)
- u_state (0-1 for hover)
- u_time




Building the app:

        mkdir build && cd build && cmake .. && make -j

or
        cmake -B build -DCMAKE_BUILD_TYPE=Release

or with presets:

        cmake --preset default
        cmake --build build -j


